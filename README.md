# Sector 317

Indoor tactical simulation experience — React + Vite + Supabase.
**Live at: https://sector317.com**

## Project Structure

```
sector317-project/
├── public/
│   ├── favicon.ico           brand favicon (317)
│   ├── favicon-256.png       hi-res favicon
│   ├── hero.png              🚫 never overwrite
│   ├── logo.png              🚫 never overwrite
│   └── leaderboard.html      public leaderboard (no login required)
├── src/
│   ├── App.jsx               main app — overwrite each session
│   ├── LandingPage.jsx       landing page — overwrite each session
│   ├── main.jsx              entry point — rarely changes
│   └── supabase.js           DB layer — overwrite each session
├── index.html                overwrite each session
├── vercel.json               overwrite each session
├── schema-patch-oauth.sql    run ONCE in Supabase SQL Editor
├── schema-runs-scores-leaderboard.sql  run ONCE in Supabase SQL Editor
├── vite.config.js            rarely changes
├── package.json              overwrite if deps change
├── .env.example              reference only — never commit .env.local
└── .gitignore
```

## ✅ ALWAYS OVERWRITE (from zip each session)
- src/App.jsx
- src/LandingPage.jsx
- src/supabase.js
- index.html
- vercel.json
- public/leaderboard.html
- public/favicon.ico
- public/favicon-256.png
- README.md

## ⚠️ OVERWRITE ONLY IF TOLD
- package.json (only when dependencies change)
- vite.config.js
- .gitignore
- schema-*.sql (run once in Supabase, then leave alone)

## 🚫 NEVER OVERWRITE
- .env.local (your private keys — not in this zip)
- package-lock.json (generated by npm)
- node_modules/ (never in zip)
- public/logo.png
- public/hero.png

## Development

```bash
npm install
npm run dev
```

## Environment Variables (.env.local)

```
VITE_SUPABASE_URL=your_project_url
VITE_SUPABASE_ANON_KEY=your_anon_key
```

## Database — Run SQL in Order (once each)
1. `schema-patch-oauth.sql`
2. `schema-runs-scores-leaderboard.sql`

## Public Leaderboard
Visit https://sector317.com/leaderboard.html
Before first deploy: replace the two placeholder values at the bottom of
`public/leaderboard.html` with your actual Supabase URL and anon key.

## Demo Accounts
| Phone      | Role     |
|------------|----------|
| 3175550101 | Customer |
| 3175550201 | Staff    |
| 3175550204 | Manager  |
| 3175550001 | Admin    |
